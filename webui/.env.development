# Vite proxies /api, /media and /thumbs to the Python app — see
# vite.config.ts. Run the backend with:
#   conda run -n krea2 python scripts/dryrun.py --features all --api-only
VITE_API_BASE=/api/v1
